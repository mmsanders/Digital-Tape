# WP-05 — Parts order #1

**Stream:** hardware · **Owner:** Hardware Lead drafts, Michael orders · **Status:** in flight
**Depends on:** nothing · **Blocks:** WP-04 (bench validation), WP-20, the issue #5 measurement

## Interface

Two carts Michael can check out without deciding anything, and one measurement tool that turns
the second cart into the answer to issue #5.

**Every order goes to Michael first** (Decisions 002 §4) — prepared as a checkout-ready cart
with a one-line justification. He approves; nothing is placed on my authority.

**C-60 changed this cart.** The standard tape is 60 minutes (ADR-018), so the copy needs
21.2 MB/s, not 31.75 — **inside every V30 card's floor with ~40 % margin, on plain high-speed
4-bit**. So: **V30 rather than A2, and 32 GB is enough.** Characterisation is now a *headroom
measurement*, not a gate, and it can travel with the bench order rather than going first.

**Everything else here is `UNVERIFIED`.** No distributor, manufacturer or fab domain is reachable
from the Hardware Lead's environment (STATUS-HARDWARE H-02), so prices are estimates and stock
is unchecked. Part *choices* are sound; part *availability* has not been confirmed by anyone.
Michael will find out at checkout, which is the wrong place to find out and is the cost of the
egress gap.

---

## Order 1a — card characterisation · ~$115 · **order this one first**

PM Decisions 001 §1 and §9 step 1: buy these now, do not wait on Q-002. This cart answers
whether a 90-minute cartridge is reachable, and that answer feeds Michael's Q-002.

Six SKUs, chosen to span controller vendors rather than to span price:

| # | Card | Why this one | Est. |
|---|---|---|---|
| 1 | SanDisk Extreme Pro microSDXC 128 GB (A2 V30 U3) | The default premium pick; reviewers measure ~100 MB/s sequential write | $22 |
| 2 | SanDisk Extreme microSDXC 128 GB (A2 V30) | Same vendor, lower bin — isolates *bin* from *vendor* | $16 |
| 3 | Samsung PRO Plus microSDXC 128 GB | Different controller lineage; ~88 MB/s held over a 25 GB transfer in published tests | $18 |
| 4 | Lexar Professional Silver Plus microSDXC 128 GB | Reviewed as the fastest UHS-I sustained writer available | $18 |
| 5 | Kingston Canvas Go! Plus 128 GB | A fourth controller vendor, and the most likely to have an industrial sibling later | $16 |
| 6 | **A cheap no-name V30, ~$10** | **Expected to fail.** See below | $10 |
| — | USB 3.2 UHS-I card reader, ≥ 150 MB/s rated | **Not optional.** Many readers cap near 90 MB/s and would silently become the thing we measure | $15 |

**Why buy a card we expect to fail.** If all six pass, we have not learned whether the test can
discriminate — we have learned that the bar is low. A card that is genuinely marginal proves the
measurement has teeth. It is the same discipline as bracketing a print sweep wide enough that
the endpoints are obviously wrong: a test where everything passes told you nothing.

**Sources for the write-speed claims:**
[Alik Griffin, 41 cards speed-tested](https://alikgriffin.com/micro-sd-memory-cards/) ·
[Camera Memory Speed, SanDisk Extreme V30 A2](https://www.cameramemoryspeed.com/reviews/sd-cards/sandisk-extreme-plus-v30-a2-128gb-microsdxc-memory-card/) ·
[Camera Memory Speed, Samsung PRO Plus](https://www.cameramemoryspeed.com/reviews/sd-cards/samsung-pro-plus-64gb-micro-sdxc-memory-card/)

### What the published numbers already suggest

Reviewers measure 80–100 MB/s sustained sequential write on good UHS-I cards, against a
requirement of 31.75 MB/s. **That is 2.5–3× margin, and it means 90 minutes is likely to
survive.** Two reasons to measure anyway rather than take the win:

1. Those benchmarks run on a PC reader at SDR104, on a card that is not 80% full, over
   transfers that may fit inside the card's SLC cache. All three flatter the number.
2. The requirement is not "a good card is fast." It is "**these exact SKUs**, at 80% full,
   worst-case window." Only a measurement produces that, and only a recorded measurement
   survives a silent controller revision inside the SKU.

### Bus headroom — worth knowing before the measurement comes back

| Rung | Bus ceiling | 90 min (953 MB) | 60 min (635 MB) |
|---|---:|---|---|
| SDR104 | 104 MB/s | comfortable | comfortable |
| SDR50 | 50 MB/s | 63% utilisation — feasible | 42% — comfortable |
| High-speed 4-bit | 25 MB/s | **impossible** (charter already prices this at 43 s) | 25.4 s — passes, barely |

**The useful conclusion: a 60-minute cartridge survives the total loss of UHS-I.** Even the
bottom rung of the sanctioned ladder meets 30 s at 635 MB. A 90-minute cartridge requires at
least SDR50 to work. That is a cleaner statement of the risk than "90 minutes needs the hard
circuit", and it is worth Michael having when he answers Q-002.

---

## Order 1b — bench build · ~$152 · for WP-04 and WP-20

| Item | Qty | Note | Est. |
|---|---:|---|---|
| Teensy 4.1 | 1 | Built-in microSD on 4-bit SDIO | $32 |
| Teensy Audio Shield Rev D | 1 | SGTL5000 — same codec as the production board, so codec bring-up is not new work later. Rev D is current as of Feb 2025 and widely stocked; the 2023 Rev D2 detour was a 32-pin shortage, now past | $17 |
| microSD breakout (SPI) | 1 | Second slot. See the limitation below | $8 |
| Tactile momentary switches | 20 | Route A carriers, plus spares — they get destroyed in a latch sweep | $8 |
| Push-push (alternate-action) switches | 6 | Route B evaluation | $8 |
| Small push/pull solenoids, **≤ 125 mJ per actuation** | 4 | The energy budget selects these now, not force alone — ADR-116. Four so Route B can be tried rather than argued about | $40 |
| Logic-level MOSFETs + flyback diodes | 10 | AO3400 / IRLZ44N class | $10 |
| 74HC221 (non-retriggerable one-shot) | 4 | The solenoid protection in `thermal-budget.md` §6 | $6 |
| PPTC assortment, ~100 mA hold | 10 | The duty-cycle backstop | $6 |
| Protoboard, headers, hookup wire | — | | $17 |

### A limitation worth stating before Michael spends the money

**The bench build cannot validate the 30-second copy, and it should not be asked to.** The
Teensy 4.1 has one 4-bit SDIO port (its built-in socket). A second card has to hang off SPI at
roughly 10–20 MB/s, so a bench copy of a 90-minute cartridge would take 60–95 s no matter how
good the cards are. That is a property of the bench platform, not a finding about the design.

So the two jobs split:

- **Card characterisation → a PC with the reader in order 1a.** Faster to set up, cleaner data,
  and available the day the cards arrive rather than after WP-17.
- **Bench build → function, not throughput.** Hot-swap detect, the LED row, the button matrix,
  the interlock, the solenoid one-shot. All of WP-18 and WP-20 except the number.

This should be reflected in WP-18's acceptance criteria, which currently read as though the
bench proves the copy time. Flagged to the PM in `docs/REVIEW/hardware-lead.md`.

---

## Order 1c — **CANCELLED** (PM Decisions 003 §2)

There is no lead-time risk to hedge. The 39-week / 490-MOQ figure I found is the **tray** variant,
factory-direct. The PM checked the reel suffix and found **`SGTL5000XNBA3R2` — nearly 8,900 at
Mouser, ~$7.89 in singles, cut tape.** Same die, different packaging. Buy what a build needs when
a build needs it.

Same answer for the RT1062: `MIMXRT1062DVL6B` shows thousands across Future, Arrow, Newark and
Verical at $6–12 in small quantities. The question I flagged as unasked is asked and it is fine.

**Two things carried forward from it, because they are still true:**

**Record the SKU with its suffix.** `XNBA3` and `XNBA3R2` are the same die and **not
interchangeable line items** — one is tray, one is reel, and only one of them is in stock
anywhere. A BOM that says "SGTL5000" is not orderable. `spec/hw/board-rev-a.md` §6 carries the
field.

**The second source stands on its own merits** (ADR-115). `SGTL5000XNLA3R2` is the same silicon
in a 20-pin package, so a supply failure costs a footprint change and no firmware work. That was
never really a lead-time hedge; it is cheap optionality, and the JLCPCB stock depth (1205 versus
74) argues for it independently of anything Mouser holds.

**What I could not verify myself.** `www.mouser.com`, `www.octopart.com` and `www.ti.com` remain
blocked; `www.findchips.com` returns a JavaScript shell to a non-browser client, as the PM
predicted for DigiKey. So the Mouser R2 line is the **PM's reading, not mine** — recorded as such
rather than as confirmed. `www.nxp.com` and `www.lcsc.com` did open, which is what closed the
lifecycle question.

## Spending

| | |
|---|---|
| Order 1a | ~$115 — under the $150 per-order authority |
| Order 1b | ~$152 — **$2 over.** Different vendor (PJRC vs. general retail), so it is a genuinely separate order, but it needs a nod rather than an assumption |
| ~~Order 1c~~ | **cancelled** — no lead-time risk to hedge (Decisions 003 §2) |
| Cumulative | **~$267**. Every order goes to Michael for approval (Decisions 002 §4) |

Splitting is not an attempt to slide under the limit — these are two carts from two vendors with
two different urgencies. Order 1a is on the critical path for issue #5 and should go today.
Order 1b can wait for Q-006, since the switches and solenoids are useless until there are
printed carriers to put them in.

Running total is tracked in `docs/STATUS-HARDWARE.md`.

---

## Acceptance criteria

| # | Criterion | Checkable by |
|---|---|---|
| A-1 | Six card SKUs and a ≥150 MB/s reader in hand; exact SKU **and** revision recorded for each | Anyone reading `hardware/characterisation/results/` |
| A-2 | Each card measured with `measure_sustained_write.py`: filled to 80%, ≥1200 MB transfer, worst 64 MB window reported | The committed JSON per card |
| A-3 | Result recorded against the PM's rule: **≥2 SKUs at ≥35 MB/s worst-case → 90 minutes stands; otherwise 60 minutes** | PM Decisions 001 §1 |
| A-4 | The reader is ruled out as the bottleneck — the fastest card is not pinned to the reader's ceiling | Compare the top two cards' worst windows |
| A-5 | Bench parts sufficient to build one Route A carrier and one Route B carrier | WP-04 |

A-4 is the one an eager tester skips. If the two fastest cards report suspiciously similar
numbers, the reader is the instrument limit and the measurement is void.

## How to run it

```
# one card, filled first, result committed
./hardware/characterisation/measure_sustained_write.py /mnt/card \
    --sku "MB-MD128SA/AM" --revision "<CID>" --reader "<reader model>" \
    --fill --json hardware/characterisation/results/samsung-pro-plus-128.json
```

Exit status is 0 for PASS and 2 for FAIL, so the whole set can be run from a loop and the
verdicts read off at the end.

## Independent sign-off

Per PM Decisions 001 §4: the *criteria* above go to the Verification Lead before the measurement
is run — the failure mode with a self-designed test is the criterion, not the number. The
measurement itself is self-reported with data attached, and every result file carries the raw
per-window series, not just the verdict.

**Status: `SELF-REPORTED — no independent confirmation`** until the Verification Lead has read
A-1…A-5.
