# Michael — cloud account setup

The current complete instructions are [CLOUD-HOOKUP.md](CLOUD-HOOKUP.md).
Create DT PM (Astra), DT Software Lead (Opus), DT Hardware Lead (Opus),
DT Verification Lead (Sol), DT Surge Lead (Grok Bot), and DT Grok Worker.
Paste each linked role manifest's setup-only bootstrap. Return non-secret instance
ids/URLs and capability reports; keep tokens in private credential stores.

The human environment gate is already verified. No need to repeat that setup.
Distinct role identities and a real supported wake/claim/return adapter are still needed.
Do not enable scheduled model polling, substitute providers, or change runtime actor
strings to Michael merely to make an unconfigured service run.
